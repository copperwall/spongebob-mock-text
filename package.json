{
  "name": "mock-text-node",
  "version": "0.0.1",
  "description": "A module to easily make a webpage's text be mocked like that one spongebob meme.",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Opperwall",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.7.4",
    "@babel/core": "^7.7.4"
  }
}
